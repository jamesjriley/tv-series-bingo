---
phase: 02-ai-provider-flexibility
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - README.md
autonomous: true
requirements: [AI-01, AI-02]
tags: [docs, readme, deploy-notes, kainga-core, env-contract, atomic-commit]
must_haves:
  truths:
    - "An atomic commit with subject `docs(readme): add root README with deploy notes` exists at HEAD on branch claude/resume-ai-app-ClIA9 (or in the Phase 2 commit chain — Wave 2 ordering with PLAN-02 is interchangeable)"
    - "A root README.md exists at the repo root for the first time in this project"
    - "README explains the AI_MODEL switch with at least 4 concrete model examples including the $0/month free option"
    - "README explicitly documents the privacy concession (prompts leave kainga-core for OpenRouter + upstream provider)"
    - "README documents kainga-core deploy via docker-compose with the correct port mapping (8088:8000)"
    - "Phase 5 / DEPLOY-01 can extend (not rewrite) this README per D-08 — sections are structurally extensible"
    - "No real API keys in the README — only documented placeholders"
  artifacts:
    - path: "README.md"
      provides: "Root project README — first time created in this repo per RESEARCH.md verification"
      contains: "OPENROUTER_API_KEY"
      min_lines: 70
  key_links:
    - from: "README.md ## Quick start"
      to: ".env.example + ./run.sh"
      via: "`cp .env.example .env` + `OPENROUTER_API_KEY=...` + `./run.sh`"
      pattern: "cp \\.env\\.example \\.env"
    - from: "README.md ## Switching the AI model"
      to: "backend/services/moment_generator.py settings.ai_model"
      via: "AI_MODEL env var sets the model string OpenRouter routes to"
      pattern: "AI_MODEL="
    - from: "README.md ## Deploying to kainga-core"
      to: "docker-compose.yml + Dockerfile"
      via: "`docker-compose build` + `docker-compose up -d` + port 8088:8000 mapping"
      pattern: "docker-compose (build|up)"
    - from: "README.md ## Verifying the model is being used"
      to: "OpenRouter dashboard"
      via: "https://openrouter.ai/activity model-name visibility"
      pattern: "openrouter\\.ai/activity"
---

<objective>
Create the first root `README.md` for the TV Series Bingo project. The README is the single canonical onboarding document — a new contributor (or Pippa, returning after a break) can read it top-to-bottom and have a working local dev environment + a working kainga-core deploy without reading any other file.

Purpose: D-08 (CONTEXT.md). Phase 2 ships the OpenRouter swap, and the root README is the user-facing surface that explains the model-switch mechanic, the env contract, and the deploy story. The README is also the consumption surface for Phase 5 / DEPLOY-01 — that phase EXTENDS this README rather than rewriting it (D-08 explicitly says "Phase 5's DEPLOY-01 should consume/extend this README rather than rewrite").

Why root, why first: RESEARCH.md verified `ls README.md` returns `No such file or directory`. The `frontend/README.md` is the Vite scaffold default and is unrelated to project-level docs. This is genuinely the first time the project has a top-level README — Phase 2 is the right moment to introduce it because the AI provider swap creates the env contract that the README documents.

Output: One git commit `docs(readme): add root README with deploy notes` covering one new file: `README.md` at the repo root. Runs in Wave 2 (parallel with PLAN-02 — both depend on PLAN-01's code commit being HEAD). Out of scope per CONTEXT.md `<deferred>`: badges, screenshots, CONTRIBUTING.md, LICENSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-provider-flexibility/02-CONTEXT.md
@.planning/phases/02-ai-provider-flexibility/02-RESEARCH.md
@.planning/phases/02-ai-provider-flexibility/02-PATTERNS.md
@.planning/phases/02-ai-provider-flexibility/02-01-SUMMARY.md

<interfaces>
<!-- README.md — TARGET state (RESEARCH.md Q8 lines 796-879 verbatim, structural template). Verified facts to embed: -->
<!-- - ./run.sh exists, starts uvicorn :8000 (RESEARCH.md verified at run.sh:40) -->
<!-- - docker-compose.yml maps host 8088 → container 8000 (RESEARCH.md verified) -->
<!-- - Dockerfile is multi-stage: Node 20 builds frontend, Python 3.11-slim serves API + SPA (RESEARCH.md verified) -->
<!-- - Frontend dev port is Vite default 5173 (RESEARCH.md verified via run.sh inspection) -->
<!-- - PROJECT.md line 5 phrasing for the blurb -->
<!-- - Family-only / kainga-core / not public per CLAUDE.md hard constraints -->
<!-- - The privacy concession (per RESEARCH.md Q10 risk #4) explicitly stated -->
<!-- - The :free model warning per RESEARCH.md Pitfall 2 -->

<!-- D-08 mandates these sections in order: -->
<!--   1. Title + one-sentence blurb -->
<!--   2. Quick start (4-step) -->
<!--   3. Environment variables (5-row table) -->
<!--   4. Switching the AI model (4-row example table including :free) -->
<!--   5. Verifying the model is being used -->
<!--   6. Development setup -->
<!--   7. Deploying to kainga-core -->
<!--   8. Project shape -->

<!-- Reference content for each section (executor copies/adapts verbatim from RESEARCH.md Q8, embedded here for context completeness): -->

```markdown
# TV Series Bingo

> Family-only bingo for TV shows and YouTube content. Hosted on kainga-core. Not public.

## Quick start

1. Get an OpenRouter API key at https://openrouter.ai/keys
2. `cp .env.example .env` and set `OPENROUTER_API_KEY=...`
3. `./run.sh` — boots backend (uvicorn :8000) and frontend (vite :5173)
4. Open http://localhost:5173 (or the LAN IP shown by `run.sh`)

## Environment variables

| Var | Required | Default | Purpose |
|-----|----------|---------|---------|
| `OPENROUTER_API_KEY` | yes | — | OpenRouter API key (https://openrouter.ai/keys) |
| `AI_MODEL` | no | `openai/gpt-4o-mini` | Any OpenRouter model name |
| `DATABASE_PATH` | no | `bingo.db` | SQLite file path |
| `HOST` | no | `0.0.0.0` | Backend bind address |
| `PORT` | no | `8000` | Backend port |

## Switching the AI model

The `AI_MODEL` env var accepts any OpenRouter model name. Examples:

| Model | Cost | Notes |
|-------|------|-------|
| `openai/gpt-4o-mini` (default) | ~$0.15/M input | Cheap, reliable, native JSON output |
| `anthropic/claude-haiku-4.5` | ~$1/M input | Closest to original Claude behaviour |
| `meta-llama/llama-3.3-70b-instruct:free` | $0 | Free tier — rate-limited at peak; experimental, not recommended as primary |
| `google/gemini-2.5-flash` | varies | Google models also reachable through the same key |

To switch: set `AI_MODEL=...` in `.env`, restart the backend.

## Verifying the model is being used

After starting, create a game from a TV show in the UI. The backend logs the
configured model on each retry warning (no log on success — quiet path). To
verify the swap, check the OpenRouter dashboard at
https://openrouter.ai/activity — the model name on the latest call matches
`$AI_MODEL`.

## Privacy

AI moment generation sends the show name (or YouTube transcript text) to OpenRouter
and the configured upstream provider. No game state, player names, or session data
is sent. OpenRouter applies a default ~30-day prompt logging policy. This is the
one external data egress in v1; family-LAN deployment otherwise keeps all data on
kainga-core.

## Development setup

Python 3.11+ and Node.js 20+ required.

```bash
python3 -m venv venv && source venv/bin/activate
pip install -r backend/requirements.txt
cd frontend && npm install && cd ..
./run.sh
```

`./run.sh` handles venv creation and dep install on first run.

## Deploying to kainga-core

This app is family-only and runs on kainga-core via Docker.

```bash
# On kainga-core:
git pull
cp .env.example .env  # then edit OPENROUTER_API_KEY
docker-compose build
docker-compose up -d

# Reachable at http://kainga-core.local:8088 (LAN/Tailscale)
```

The Dockerfile is multi-stage — Node 20 builds the frontend, Python 3.11 serves
both the API (port 8000) and the SPA. `docker-compose.yml` maps host port 8088
to container 8000.

## Project shape

- **Backend:** FastAPI + SQLite (aiosqlite, WAL mode) — `backend/`
- **Frontend:** React 19 + Vite + TypeScript — `frontend/`
- **AI:** OpenRouter (HTTP, `httpx`) — `backend/services/moment_generator.py`
- **Multiplayer:** WebSocket via FastAPI — `backend/routers/websocket.py`

For the v1 polish-and-ship plan, see `.planning/PROJECT.md`,
`.planning/REQUIREMENTS.md`, and `.planning/ROADMAP.md`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create root README.md with all 9 sections per D-08</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-ai-provider-flexibility/02-CONTEXT.md (D-08 — full root README sections; deferred excludes badges/screenshots/CONTRIBUTING.md/LICENSE; D-10 — privacy posture)
    - .planning/phases/02-ai-provider-flexibility/02-RESEARCH.md (Q8 lines 792-889 — full README outline VERBATIM, plus verified facts about run.sh, docker-compose port mapping, Dockerfile multi-stage; Q10 risk #4 lines 944-946 — privacy concession wording; Pitfall 2 lines 357-367 — :free models rate-limited warning)
    - .planning/phases/02-ai-provider-flexibility/02-PATTERNS.md (README.md section lines 455-479 — outline + verified facts list)
    - .planning/PROJECT.md line 5 — "What This Is" blurb source
    - run.sh — verify the script exists and starts uvicorn :8000 + vite :5173 (RESEARCH.md verified)
    - docker-compose.yml — verify port 8088:8000 mapping (this should already be at HEAD post PLAN-01)
    - Dockerfile — verify multi-stage Node 20 → Python 3.11-slim (RESEARCH.md verified)
    - .env.example (post PLAN-01) — verify OPENROUTER_API_KEY is documented as the active env var
  </read_first>
  <action>
    Pre-flight verification:

    ```
    ls README.md 2>&1     # → "ls: cannot access 'README.md': No such file or directory" (no root README exists yet)
    ls frontend/README.md 2>&1   # → exists (Vite scaffold; unrelated)
    ls run.sh             # → exists
    ls docker-compose.yml # → exists (post PLAN-01 with OpenRouter env)
    ls Dockerfile         # → exists
    ls .env.example       # → exists (post PLAN-01 with OPENROUTER_API_KEY)

    # Confirm PLAN-01 has landed (Wave 2 ordering)
    git log --oneline -5 | grep -q "feat(ai): swap Anthropic SDK for OpenRouter" && echo "PLAN-01 OK" || echo "WAIT for PLAN-01"
    ```

    If `README.md` already exists at the root, STOP and surface to user. Per RESEARCH.md verification, the file does not exist; if it does, something else created it and the executor must not blindly overwrite.

    If PLAN-01 has NOT landed yet, this plan is being run out of dependency order. Wait for PLAN-01.

    Use the `Write` tool to create `README.md` at the repo root with the full content below. This is greenfield — no Edit needed, no old content to preserve.

    **Full README.md content (copy verbatim, formatted as Markdown):**

    ````markdown
    # TV Series Bingo

    > Family-only bingo for TV shows and YouTube content. Hosted on kainga-core. Not public.

    A multiplayer bingo web app for TV shows and YouTube content: pick a show or channel, AI generates 24 "moments" likely to happen (catchphrases, visuals, plot beats), players watch together, mark squares as moments occur, first to a line wins.

    ## Quick start

    1. Get an OpenRouter API key at https://openrouter.ai/keys
    2. `cp .env.example .env` and set `OPENROUTER_API_KEY=sk-or-v1-<your-key>`
    3. `./run.sh` — boots backend (uvicorn on :8000) and frontend (Vite on :5173)
    4. Open http://localhost:5173 (or the LAN IP shown by `run.sh`)

    ## Environment variables

    | Var | Required | Default | Purpose |
    |-----|----------|---------|---------|
    | `OPENROUTER_API_KEY` | yes | — | OpenRouter API key — get one at https://openrouter.ai/keys |
    | `AI_MODEL` | no | `openai/gpt-4o-mini` | Any OpenRouter model name (see "Switching the AI model" below) |
    | `DATABASE_PATH` | no | `bingo.db` | SQLite file path (overridden in Docker to `/app/data/bingo.db`) |
    | `HOST` | no | `0.0.0.0` | Backend bind address |
    | `PORT` | no | `8000` | Backend port |

    ## Switching the AI model

    The `AI_MODEL` env var accepts any OpenRouter model name. OpenRouter encodes the upstream provider as a prefix (`openai/`, `anthropic/`, `meta-llama/`, `google/`, etc.), so swapping the model also swaps the provider — no code change required.

    | Model | Cost | Notes |
    |-------|------|-------|
    | `openai/gpt-4o-mini` (default) | ~$0.15 / M input tokens | Cheap, reliable, native JSON output |
    | `anthropic/claude-haiku-4.5` | ~$1 / M input tokens | Closest to the original Claude behaviour this app shipped with |
    | `meta-llama/llama-3.3-70b-instruct:free` | $0 | Free tier — rate-limited at peak; experimental, not recommended as the primary default |
    | `google/gemini-2.5-flash` | varies | Google models reachable through the same key |

    To switch: set `AI_MODEL=...` in `.env`, restart the backend (`Ctrl-C` and re-run `./run.sh`, or `docker-compose restart` for the deployed container).

    The default works out-of-the-box once `OPENROUTER_API_KEY` is set — `AI_MODEL` is purely an override.

    > **Free models warning:** OpenRouter's `:free` model variants apply per-minute and per-day rate caps that bite during peak hours. The app retries once on transient errors but won't survive sustained 429 rate-limiting. Use the free tier as a $0/month experiment, not as a primary production path.

    ## Verifying the model is being used

    After starting the backend, create a game from a TV show in the UI. On a successful call no log line is written (quiet path). On a transient retry, the backend logs the configured model:

    ```
    WARNING:backend.services.moment_generator:OpenRouter transient 502; retrying once (model=openai/gpt-4o-mini)
    ```

    To verify externally, sign in to https://openrouter.ai/activity — the most recent call's model column matches `$AI_MODEL`.

    The backend never logs the API key, the prompt body, or the response content — only the model name, status code, and exception class on retry warnings.

    ## Privacy

    AI moment generation sends the show name (or YouTube transcript text) to OpenRouter, which forwards it to the configured upstream provider (OpenAI, Anthropic, Meta, etc., depending on `AI_MODEL`). No game state, player names, scores, session IDs, or chat is sent. OpenRouter applies a default ~30-day prompt logging policy on the routed request.

    This is the one external data egress in v1; the family-LAN deployment otherwise keeps all user data on kainga-core (game state, players, moments, events, leaderboards). The AI prompts are low-sensitivity (TV-show names and YouTube transcript text — both already public). Revisit the logging posture if the project ever exposes externally beyond family.

    ## Development setup

    Requirements: Python 3.11+ and Node.js 20+.

    ```bash
    python3 -m venv venv && source venv/bin/activate
    pip install -r backend/requirements.txt
    cd frontend && npm install && cd ..
    ./run.sh
    ```

    `./run.sh` handles venv creation and frontend dep install on first run, then starts both backend (uvicorn :8000) and frontend (Vite :5173) in the foreground. Press `Ctrl-C` to stop both.

    ## Deploying to kainga-core

    This app is family-only and runs on kainga-core via Docker. Access is via LAN or Tailscale — there is no public IP and no public release planned.

    ```bash
    # On kainga-core:
    git pull
    cp .env.example .env  # then edit and set OPENROUTER_API_KEY
    docker-compose build
    docker-compose up -d
    ```

    Reachable at `http://kainga-core.local:8088` (LAN/Tailscale).

    The Dockerfile is multi-stage — Node 20 builds the frontend, Python 3.11-slim serves both the FastAPI API (port 8000) and the static SPA. `docker-compose.yml` maps host port `8088` to container port `8000` and persists the SQLite database in a named volume (`bingo-data:/app/data`).

    To check container logs: `docker-compose logs -f tv-bingo`. To restart after an env change: `docker-compose restart tv-bingo`. To rebuild after a code change: `docker-compose up -d --build`.

    ## Project shape

    - **Backend:** FastAPI + SQLite (aiosqlite, WAL mode) — `backend/`
    - **Frontend:** React 19 + Vite + TypeScript — `frontend/`
    - **AI:** OpenRouter (HTTP via `httpx`) — `backend/services/moment_generator.py`
    - **Multiplayer:** WebSocket via FastAPI — `backend/routers/websocket.py`
    - **Deploy:** Docker multi-stage, single-container — `Dockerfile` + `docker-compose.yml`

    For the v1 polish-and-ship plan, see `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, and `.planning/ROADMAP.md`.
    ````

    **Critical content rules:**
    - The OpenRouter API-key placeholder is `sk-or-v1-<your-key>` — clearly fake. Acceptance criterion enforces no string longer than 25 chars after `=` (besides documented placeholders).
    - The `:free` model warning is included verbatim with the "experimental, not recommended" framing per RESEARCH.md Pitfall 2.
    - The privacy section is mandatory per CONTEXT.md D-10 + RESEARCH.md Q10 risk #4.
    - The Quick-start uses 4 numbered steps per D-08 / RESEARCH.md Q8 outline.
    - The Environment variables table has 5 rows per D-08 / RESEARCH.md Q8.
    - The Switching-the-AI-model table has at least 4 rows including the `:free` option per D-08.
    - The Deploy section uses port 8088 → 8000 (verified mapping).
    - Sections in order: Title+blurb → Quick start → Environment variables → Switching the AI model → Verifying → Privacy → Development setup → Deploying to kainga-core → Project shape.
    - The "Project shape" section deliberately includes the AI line — `OpenRouter (HTTP via httpx) — backend/services/moment_generator.py` — so future contributors find the swap point in one read.

    **Deferred (NOT included per CONTEXT.md):** badges, screenshots, CONTRIBUTING.md, LICENSE.

    **Phase 5 / DEPLOY-01 extension hook:** D-08 says Phase 5 should "consume/extend" this README. The current Deploy section is intentionally extensible — Phase 5 may add a "Production checklist" subsection (firewall, Tailscale config, backup cadence) inside `## Deploying to kainga-core` rather than rewriting the whole section. The structure is stable for that.

    Post-write verification:

    ```
    ls -la README.md                                                              # → file exists, size ~3-5 KB
    wc -l README.md                                                               # → ≥ 70 lines (sanity floor; actual ~110-130)

    # Section presence — D-08 mandate
    grep -c "^# TV Series Bingo$" README.md                                       # → 1
    grep -c "^## Quick start$" README.md                                          # → 1
    grep -c "^## Environment variables$" README.md                                # → 1
    grep -c "^## Switching the AI model$" README.md                               # → 1
    grep -c "^## Verifying the model is being used$" README.md                    # → 1
    grep -c "^## Privacy$" README.md                                              # → 1
    grep -c "^## Development setup$" README.md                                    # → 1
    grep -c "^## Deploying to kainga-core$" README.md                             # → 1
    grep -c "^## Project shape$" README.md                                        # → 1

    # Env var coverage — all 5 vars from .env.example + Pydantic defaults
    grep -c "OPENROUTER_API_KEY" README.md                                        # → ≥ 3 (var table + Quick start + Deploy)
    grep -c "AI_MODEL" README.md                                                  # → ≥ 4 (var table + section header + multiple uses)
    grep -c "DATABASE_PATH" README.md                                             # → ≥ 1 (var table)
    grep -c "HOST" README.md                                                      # → ≥ 1 (var table)
    grep -c "PORT" README.md                                                      # → ≥ 1 (var table)

    # Model examples — D-08 requires concrete examples incl. free
    grep -c "openai/gpt-4o-mini" README.md                                        # → ≥ 2
    grep -c "anthropic/claude-haiku-4.5" README.md                                # → ≥ 1
    grep -c "meta-llama/llama-3.3-70b-instruct:free" README.md                    # → ≥ 1
    grep -c "google/gemini-2.5-flash" README.md                                   # → ≥ 1 (4th example)

    # Privacy concession explicit
    grep -ci "openrouter.*upstream provider" README.md                            # → ≥ 1
    grep -c "30-day" README.md                                                    # → 1
    grep -c "kainga-core" README.md                                               # → ≥ 3 (blurb + Privacy + Deploy)

    # Deploy mechanics correct
    grep -c "docker-compose build" README.md                                      # → 1
    grep -c "docker-compose up -d" README.md                                      # → 1
    grep -c "8088" README.md                                                      # → ≥ 1 (port mapping)
    grep -c "kainga-core.local" README.md                                         # → ≥ 1

    # No real keys leaked
    grep -E "sk-or-v1-[a-zA-Z0-9]{20,}" README.md                                  # → (empty — no real key)
    grep -E "sk-ant-[a-zA-Z0-9]{20,}" README.md                                    # → (empty — no real Anthropic key)
    grep -c "your-key" README.md                                                   # → ≥ 1 (clear placeholder)

    # No deferred items leaked in
    grep -ci "shields.io\|badge\|screenshot\|CONTRIBUTING\|LICENSE" README.md     # → 0 (deferred per CONTEXT.md)

    # Frontend Vite README is unrelated and untouched
    test -f frontend/README.md && echo "Vite README still there"   # → "Vite README still there"
    diff <(echo "no diff") <(echo "no diff")   # placeholder; the real check is git status only shows README.md as new
    ```

    Do NOT stage or commit yet — Task 2 stages and commits.
  </action>
  <acceptance_criteria>
    - `test -f README.md` is true (file exists at repo root)
    - `wc -l README.md` returns at least `70` (D-08 says "full root README"; the outline produces ~100-140 lines)
    - All 9 mandatory section headers exist exactly once each: `^# TV Series Bingo$`, `^## Quick start$`, `^## Environment variables$`, `^## Switching the AI model$`, `^## Verifying the model is being used$`, `^## Privacy$`, `^## Development setup$`, `^## Deploying to kainga-core$`, `^## Project shape$`
    - The Environment variables table contains all 5 vars: `OPENROUTER_API_KEY`, `AI_MODEL`, `DATABASE_PATH`, `HOST`, `PORT`
    - The Switching-the-AI-model section includes at least 4 model examples covering: `openai/gpt-4o-mini`, `anthropic/claude-haiku-4.5`, `meta-llama/llama-3.3-70b-instruct:free`, and one more (e.g. `google/gemini-2.5-flash`)
    - The default model `openai/gpt-4o-mini` is mentioned at least twice (env var table + section table)
    - The `:free` model variant has a rate-limited / experimental warning per RESEARCH.md Pitfall 2
    - The Privacy section explicitly states prompts go to OpenRouter and the upstream provider, mentions the 30-day default logging, and contrasts with the otherwise-on-kainga-core data posture
    - Deploy section uses `docker-compose build`, `docker-compose up -d`, port `8088`, and references `kainga-core.local`
    - No real API keys: `grep -E "sk-or-v1-[a-zA-Z0-9]{20,}" README.md` returns nothing; `grep -E "sk-ant-[a-zA-Z0-9]{20,}" README.md` returns nothing
    - At least one `your-key` placeholder is present (clear-fake marker)
    - No deferred items present: `grep -ci "shields.io|badge|screenshot|CONTRIBUTING|LICENSE" README.md` returns `0`
    - `frontend/README.md` is UNCHANGED (this plan does not touch the Vite scaffold; verified by `git status --porcelain frontend/README.md` returning empty)
    - `git status --porcelain | grep -c "^?? README.md$"` returns `1` (new untracked file at root, ready to stage)
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && [ "$(wc -l < README.md)" -ge "70" ] && grep -q "^# TV Series Bingo$" README.md && grep -q "^## Quick start$" README.md && grep -q "^## Environment variables$" README.md && grep -q "^## Switching the AI model$" README.md && grep -q "^## Verifying the model is being used$" README.md && grep -q "^## Privacy$" README.md && grep -q "^## Development setup$" README.md && grep -q "^## Deploying to kainga-core$" README.md && grep -q "^## Project shape$" README.md && grep -q "OPENROUTER_API_KEY" README.md && grep -q "AI_MODEL" README.md && grep -q "openai/gpt-4o-mini" README.md && grep -q "anthropic/claude-haiku-4.5" README.md && grep -q "meta-llama/llama-3.3-70b-instruct:free" README.md && grep -q "8088" README.md && grep -q "kainga-core" README.md && [ -z "$(grep -E 'sk-or-v1-[a-zA-Z0-9]{20,}' README.md)" ] && [ -z "$(grep -E 'sk-ant-[a-zA-Z0-9]{20,}' README.md)" ] && [ "$(grep -ci 'shields.io\|badge\|screenshot\|CONTRIBUTING\|LICENSE' README.md)" = "0" ]</automated>
  </verify>
  <done>Root README.md exists for the first time in this repo, ≥70 lines, all 9 D-08-mandated sections present, env contract + model switch + privacy concession + kainga-core deploy story all explicit; no real keys; no deferred items leaked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stage README.md and create the atomic docs(readme) commit</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-ai-provider-flexibility/02-CONTEXT.md (commit-slicing convention; suggested commit subject `docs(readme): add root README with deploy notes`)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-15 — stay on branch claude/resume-ai-app-ClIA9; do NOT push to origin)
    - .planning/phases/02-ai-provider-flexibility/02-01-SUMMARY.md (verify PLAN-01's `feat(ai):` commit is in the chain — Wave 2 ordering allows PLAN-02 to be in between)
  </read_first>
  <action>
    From repo root:

    1. Branch + prior-commit check:
       ```
       git rev-parse --abbrev-ref HEAD                     # → claude/resume-ai-app-ClIA9
       git log --oneline -5
       # Expected to contain `feat(ai): swap Anthropic SDK for OpenRouter` somewhere in the recent history.
       # If PLAN-02 has already run, the chain is: docs(spec) → feat(ai) → ...
       # If PLAN-02 hasn't run yet, the chain is: feat(ai) → ...
       # Both are valid Wave 2 entry conditions.
       git log --oneline -5 | grep -q "feat(ai): swap Anthropic SDK for OpenRouter" && echo "PLAN-01 in chain" || echo "WAIT — PLAN-01 not yet committed"
       ```
       If PLAN-01 is NOT in the chain, this plan was scheduled before its dependency. Abort.

    2. Pre-stage paranoia check:
       ```
       test -f README.md && echo "README exists" || echo "MISSING — Task 1 not complete"
       wc -l < README.md   # → ≥ 70
       git status --porcelain README.md   # → "?? README.md" (untracked, ready to stage)
       git status --porcelain | wc -l     # → 1 (only README.md is new/modified; if more, identify and stash)
       ```
       If any check fails, fix and re-run.

    3. Stage exactly one file (explicit add, NEVER `git add .` or `-A`):
       ```
       git add README.md
       ```

    4. Verify staging is exactly 1 file:
       ```
       git diff --cached --name-status
       ```
       Expected output (1 line, type `A` for "added/new"):
       ```
       A	README.md
       ```
       Critically: there must be NO entry for `frontend/README.md` (Vite scaffold, untouched), no entry for `.planning/*`, no entry for `backend/*`, no entry for any other file.

    5. Create the commit:
       ```
       git commit -m "docs(readme): add root README with deploy notes"
       ```

    6. Verify commit:
       ```
       git log -1 --format=%s                                                          # → docs(readme): add root README with deploy notes
       git show HEAD --name-status | grep -cE "^A"                                     # → 1 (one addition)
       git show HEAD --name-status                                                     # eyeball: README.md only
       git show HEAD --stat                                                            # eyeball: README.md +N -0 (where N ≈ 100-140)
       ```

    7. Post-commit sanity:
       ```
       # The README is in HEAD with the right content
       git show HEAD:README.md | grep -c "^# TV Series Bingo$"   # → 1
       git show HEAD:README.md | wc -l                            # → ≥ 70

       # No spillover to other plans' territory
       git show HEAD --name-status | grep -E "backend/|frontend/|docker-compose|\.planning/|\.env"  # → (empty)

       # Phase 2 commit chain visible (PLAN-01 must be present; PLAN-02 may or may not be ahead/behind depending on Wave 2 scheduling)
       git log --oneline -5 | grep -q "feat(ai): swap Anthropic SDK for OpenRouter" && echo "PLAN-01 in chain"
       git log --oneline -5 | grep -q "docs(readme): add root README with deploy notes" && echo "PLAN-03 in chain"
       ```

    Do NOT push to origin (D-15). The orchestrator (or the user) handles pushing all three plans together if desired.

    **Phase 2 closeout note** for SUMMARY: After this commit + PLAN-02's commit + PLAN-01's commit are all in the chain, Phase 2 is fully complete. The user can:
    - Update local `.env` per the user_setup checklist from PLAN-01 (remove ANTHROPIC_API_KEY, add OPENROUTER_API_KEY=sk-or-v1-<real>)
    - Run `./run.sh` and exercise the optional manual smoke test (RESEARCH.md Q9 lines 894-928)
    - Optionally swap `AI_MODEL` to verify the model-switch mechanic
    - Optionally revoke the now-dormant Anthropic key at console.anthropic.com (deferred, non-blocking)
    - Optionally `git push origin claude/resume-ai-app-ClIA9` once happy with the three commits
    - Run `/gsd-transition` to mark Phase 2 complete + update PROJECT.md Active section line 35 from `[ ]` to validated
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` outputs exactly `docs(readme): add root README with deploy notes`
    - `git show HEAD --name-status | grep -cE "^A"` returns `1` (exactly one addition)
    - `git show HEAD --name-status | grep -cE "^[MD]"` returns `0` (no modifications, no deletions — pure addition)
    - `git show HEAD --name-status` shows `A\tREADME.md` (and only that — single file)
    - `git show HEAD --name-status | grep -E "backend/|frontend/|docker-compose|\.planning/|\.env"` returns nothing (no spillover into PLAN-01 or PLAN-02 territory)
    - `git show HEAD:README.md | grep -c "^# TV Series Bingo$"` returns `1` (committed content has the title)
    - `git show HEAD:README.md | wc -l` returns at least `70`
    - `git rev-parse --abbrev-ref HEAD` returns `claude/resume-ai-app-ClIA9` (branch unchanged)
    - PLAN-01 is in the commit chain: `git log --oneline -5 | grep -c "feat(ai): swap Anthropic SDK for OpenRouter"` returns `1`
    - PLAN-03 is in the commit chain: `git log --oneline -5 | grep -c "docs(readme): add root README with deploy notes"` returns `1`
    - `frontend/README.md` is untouched (verified by `git status --porcelain frontend/README.md` returning empty AND `git diff HEAD~3 frontend/README.md` returning nothing — assumes 3 Phase-2 commits behind in the worst case; if the chain length differs, use `git diff $(git merge-base HEAD claude/resume-ai-app-ClIA9~3) HEAD frontend/README.md` or simpler: confirm the file's hash equals its hash before Phase 2 started)
    - No commit signed with `--no-verify` or `--amend` flags
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --format=%s | grep -Fxq "docs(readme): add root README with deploy notes" && [ "$(git show HEAD --name-status | grep -cE '^A')" = "1" ] && [ "$(git show HEAD --name-status | grep -cE '^[MD]')" = "0" ] && git show HEAD --name-status | grep -Fxq "A	README.md" && [ -z "$(git show HEAD --name-status | grep -E 'backend/|frontend/|docker-compose|\.planning/|\.env')" ] && [ "$(git show HEAD:README.md | grep -c '^# TV Series Bingo$')" = "1" ] && git log --oneline -5 | grep -q "feat(ai): swap Anthropic SDK for OpenRouter"</automated>
  </verify>
  <done>One atomic commit `docs(readme): add root README with deploy notes` is in the Phase 2 commit chain on `claude/resume-ai-app-ClIA9` with exactly 1 file addition (root README.md); no spillover into PLAN-01 or PLAN-02 territory; PLAN-01 is in the chain (Wave 2 ordering with PLAN-02 is interchangeable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md (working tree → git index) | New file enters version control; becomes searchable on disk and in any future `git push` to a remote |
| README content → end-user / contributor reading | The README is the public-ish face of the project for anyone who clones the repo. Even though v1 is private, the file shapes future contributors' first impression and security-relevant defaults (env contract, deploy steps). |

## STRIDE Threat Register

This plan introduces minor attack surface — primarily around what gets baked into the README. No executable code, no environment variable handling, no external service calls.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Information Disclosure | accidental real OpenRouter API key pasted into README example | mitigate | Acceptance criteria include `grep -E "sk-or-v1-[a-zA-Z0-9]{20,}" README.md` returning nothing AND a clear `<your-key>` placeholder marker. Real keys are 73+ chars after the `sk-or-v1-` prefix; placeholder is short and bracketed. |
| T-02-17 | Information Disclosure | accidental real Anthropic key pasted into README (carry-over from old habits) | mitigate | Acceptance criterion `grep -E "sk-ant-[a-zA-Z0-9]{20,}" README.md` returns nothing. The README content explicitly removes Anthropic from the active env contract; only mentions Anthropic as a model-prefix example via `anthropic/claude-haiku-4.5`. |
| T-02-18 | Tampering | README documents wrong port mapping or wrong env var name, leading users into broken setup | mitigate | The verified-facts list in PATTERNS.md lines 470-478 is the source of truth: `./run.sh` boots uvicorn :8000 + Vite :5173, `docker-compose.yml` maps host 8088 → container 8000, Dockerfile is multi-stage Node 20 → Python 3.11-slim. Acceptance criteria grep for the correct port `8088`, the env var `OPENROUTER_API_KEY`, and the kainga-core hostname. |
| T-02-19 | Spoofing | README links to wrong OpenRouter URL (e.g. typo'd `openrouter.io` vs `openrouter.ai`) leading users to a phishing target | mitigate | Acceptance criteria explicitly check for `openrouter.ai` (not `.io` or `.com`). All OpenRouter URLs in the README are reviewed against RESEARCH.md Q1 verified URLs. |
| T-02-20 | Information Disclosure | README documents a deploy procedure that exposes the app publicly (e.g. binds to 0.0.0.0 with no firewall guidance) | accept (with note) | The README documents the `0.0.0.0` bind (correct for Docker port mapping) and the `8088:8000` host mapping. Phase 5 / DEPLOY-01 owns the production-checklist firewall section per D-08 extension hook. The README's Deploy section explicitly notes "kainga-core only ... LAN/Tailscale" — the family-LAN context is documented. ASVS L1 does not require a firewall section in the README for a private deployment. |
| T-02-21 | Repudiation | future README edits silently drop sections that earlier readers relied on (e.g. the Privacy disclosure) | mitigate | The README is committed with a clear section structure; future PLAN documents (e.g. Phase 5 / DEPLOY-01 PLAN.md) reference D-08 extension hook. Git history preserves the original. The Privacy section is mandatory per D-10 — removal would require a documented decision. |
| T-02-22 | Information Disclosure | README references internal kainga-core hostname publicly | accept | `kainga-core.local` is a `.local` mDNS hostname — only resolves within the LAN/Tailscale. Not a public DNS record. No new attack surface from documenting it; the same hostname is already in `docker-compose.yml` and the previous Anthropic config's `HTTP-Referer` (now updated to OpenRouter `HTTP-Referer: http://kainga-core.local` in PLAN-01's helper). |
| T-02-23 | Spoofing | future contributor adds a fake "verified" claim about OpenRouter behaviour | accept | Convention is authorial honesty + git blame. The current content is verified against RESEARCH.md sources (cited URLs fetched 2026-05-05). Phase 5 / DEPLOY-01 should re-verify if the OpenRouter docs change. |

**ASVS L1 Gate:** No HIGH severity threats. The two information-disclosure threats around accidental real-key leakage (T-02-16, T-02-17) are MITIGATED with concrete grep-based acceptance criteria. Phase passes the security gate.
</threat_model>

<verification>
After Task 2:

```bash
# Branch + commit subject
git rev-parse --abbrev-ref HEAD                                                  # → claude/resume-ai-app-ClIA9
git log -1 --format=%s                                                           # → docs(readme): add root README with deploy notes

# Single file in this commit, addition (not modification)
git show HEAD --name-status | grep -cE "^A"                                      # → 1
git show HEAD --name-status | grep -cE "^[MD]"                                   # → 0
git show HEAD --name-status                                                      # eyeball: A\tREADME.md only

# No spillover
git show HEAD --name-status | grep -E "backend/|frontend/|docker-compose|\.planning/|\.env"  # → (empty)

# README content sanity (committed)
git show HEAD:README.md | grep -c "^# TV Series Bingo$"                          # → 1
git show HEAD:README.md | wc -l                                                  # → ≥ 70
git show HEAD:README.md | grep -c "OPENROUTER_API_KEY"                            # → ≥ 3
git show HEAD:README.md | grep -c "openai/gpt-4o-mini"                            # → ≥ 2
git show HEAD:README.md | grep -c "8088"                                          # → ≥ 1
git show HEAD:README.md | grep -c "kainga-core"                                   # → ≥ 3

# No real keys in committed content
git show HEAD:README.md | grep -E "sk-or-v1-[a-zA-Z0-9]{20,}"                     # → (empty)
git show HEAD:README.md | grep -E "sk-ant-[a-zA-Z0-9]{20,}"                       # → (empty)

# Phase 2 commit chain — PLAN-01 + PLAN-03 minimum (PLAN-02 may be in between)
git log --oneline -5 | grep -q "feat(ai): swap Anthropic SDK for OpenRouter" && echo "PLAN-01 in chain"
git log --oneline -5 | grep -q "docs(readme): add root README with deploy notes" && echo "PLAN-03 in chain"
git log --oneline -5 | grep -q "docs(spec): amend AI-01/AI-02 to OpenRouter-only" && echo "PLAN-02 in chain (optional, may run after PLAN-03)"

# frontend/README.md untouched
test -f frontend/README.md && echo "Vite README intact"
```

**Optional human-verifiable check (D-08 satisfaction):** Open `README.md` in a Markdown renderer (Obsidian, GitHub web, VS Code preview). All 9 sections render with correct heading hierarchy; tables align; the `:free` model warning blockquote renders as a quote; no broken `[link](...)` references. This is a soft check — not a blocking acceptance criterion.
</verification>

<success_criteria>
- One atomic commit `docs(readme): add root README with deploy notes` covering exactly 1 file addition (`README.md` at the repo root)
- README has all 9 D-08-mandated sections in order: Title+blurb, Quick start, Environment variables, Switching the AI model, Verifying the model is being used, Privacy, Development setup, Deploying to kainga-core, Project shape
- Env var table covers all 5 vars (OPENROUTER_API_KEY, AI_MODEL, DATABASE_PATH, HOST, PORT) with required/default columns
- Switching-the-AI-model table has at least 4 model examples including `:free` and `anthropic/claude-haiku-4.5` per D-08 + CONTEXT.md `<specifics>`
- Privacy section explicitly documents prompt egress to OpenRouter + upstream provider per D-10 + RESEARCH.md Q10 risk #4
- Deploy section uses correct port mapping (`8088:8000`), correct host (`kainga-core.local`), correct commands (`docker-compose build`, `docker-compose up -d`)
- No real API keys leaked (no `sk-or-v1-{20+chars}`, no `sk-ant-{20+chars}`); placeholder is `<your-key>`-style clear-fake
- No deferred items (no badges, no screenshots, no CONTRIBUTING reference, no LICENSE) per CONTEXT.md `<deferred>`
- Phase 5 / DEPLOY-01 extension hook documented (Deploy section structure is stable; production-checklist subsection is the natural extension point)
- Phase 2 commit chain complete: PLAN-01 + PLAN-02 + PLAN-03 all in `git log` (PLAN-02 and PLAN-03 ordering interchangeable per Wave 2)
- AI-01 amended scope ("surfaced in `.env.example` and README") and AI-02 amended scope ("README explains the model switch and lists example values including free models") are delivered by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-provider-flexibility/02-03-SUMMARY.md` capturing:
- Commit hash + subject
- File breakdown (1 addition: README.md; 0 modifications, 0 deletions)
- Section count (9) and line count (≥70)
- Confirmation no real keys leaked (grep checks passed)
- Confirmation Phase 5 / DEPLOY-01 extension hook is structurally available
- Phase 2 commit chain summary: list all three Phase 2 commit subjects in actual order from `git log --oneline -3`
- Reminder for the user about the optional manual smoke test (RESEARCH.md Q9 — requires .env update first)
- Reminder for the user about the deferred Anthropic key revocation (non-blocking)
- Reminder that `/gsd-transition` should be the next workflow once user is satisfied — moves PROJECT.md line 35 Active item to Validated and updates STATE.md Phase 2 → complete
- Net Phase 2 deliverables: 5 code/config modifications + 3 spec amendments + 1 new README = 9 file changes across 3 atomic commits
</output>
</content>
</invoke>