---
phase: 01-code-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Help.tsx
  - frontend/src/App.tsx
  - frontend/src/styles/global.css
autonomous: true
requirements: [POLISH-01]
tags: [git-hygiene, atomic-commit, frontend, react, modal]

must_haves:
  truths:
    - "An atomic commit with subject `feat(help): in-app Help modal with how-to-play guidance` exists on branch claude/resume-ai-app-ClIA9"
    - "The commit contains exactly 3 files: frontend/src/pages/Help.tsx (added), frontend/src/App.tsx (modified), frontend/src/styles/global.css (modified)"
    - "frontend/src/pages/Help.tsx is no longer untracked after the commit (git status shows it as committed)"
  artifacts:
    - path: "frontend/src/pages/Help.tsx"
      provides: "Help modal React component (default export, takes onClose: () => void)"
      contains: "export default function Help"
    - path: "frontend/src/App.tsx"
      provides: "Help mount point + ? button + showHelp state"
      contains: "import Help from \"./pages/Help\""
    - path: "frontend/src/styles/global.css"
      provides: ".help-btn and .help-btn:hover CSS rules"
      contains: ".help-btn"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/Help.tsx"
      via: "import + conditional render gated on showHelp state"
      pattern: "import Help from \"./pages/Help\""
---

<objective>
Land the in-flight Help modal as an atomic commit. This is commit 1 of 5 in Phase 1 (D-14). The work is already authored — three files sit uncommitted in the working tree. The job is to verify the diff is clean, stage exactly these three files, and commit them with the locked subject line.

Purpose: POLISH-01a — clear the first slice of in-flight work so the working tree starts collapsing toward clean. Atomic-by-feature commits make the help modal trivially revertable if it ever needs to come out.
Output: One git commit on branch claude/resume-ai-app-ClIA9 containing exactly three files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-hygiene/01-CONTEXT.md
@.planning/phases/01-code-hygiene/01-PATTERNS.md

<interfaces>
<!-- Help.tsx top-level shape (already authored, lines 1-6 in the untracked file) -->

```tsx
interface Props {
  onClose: () => void;
}

export default function Help({ onClose }: Props) {
```

<!-- App.tsx existing import block (lines 3-7 in current file) — Help import is already added on line 6 of the working-tree version -->

```tsx
import Home from "./pages/Home";
import CreateGame from "./pages/CreateGame";
import Lobby from "./pages/Lobby";
import GameBoard from "./pages/GameBoard";
import Help from "./pages/Help";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify working tree state matches PATTERNS.md before staging</name>
  <files>frontend/src/pages/Help.tsx, frontend/src/App.tsx, frontend/src/styles/global.css</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (Commit 1 section, lines 84-186)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-01, D-02)
    - frontend/src/pages/Help.tsx (the untracked file — confirm it is 168 lines, default-exports `Help`, takes `Props` with `onClose`)
    - frontend/src/App.tsx (working-tree version with the 3 hunks)
    - frontend/src/styles/global.css (working-tree version with the .help-btn block)
  </read_first>
  <action>
    Run these verification commands from the repo root (working dir: /home/pippa-sofine/Documents/Obsidian/01_Projects/tv-series-bingo):

    1. `git rev-parse --abbrev-ref HEAD` — must print `claude/resume-ai-app-ClIA9` (D-15 lock). If not on that branch, STOP and surface to user — do NOT switch branches.
    2. `git status --porcelain frontend/src/pages/Help.tsx` — expect `?? frontend/src/pages/Help.tsx` (untracked)
    3. `git status --porcelain frontend/src/App.tsx` — expect ` M frontend/src/App.tsx` (modified, unstaged)
    4. `git status --porcelain frontend/src/styles/global.css` — expect ` M frontend/src/styles/global.css` (modified, unstaged)
    5. `wc -l frontend/src/pages/Help.tsx` — expect 168 lines (per PATTERNS.md verbatim count)
    6. `grep -n "export default function Help" frontend/src/pages/Help.tsx` — expect exactly one match on a line in the 1-10 range
    7. `git diff frontend/src/App.tsx | grep -E "^\+import Help from"` — expect one match: `+import Help from "./pages/Help";`
    8. `git diff frontend/src/App.tsx | grep -E "^\+.*useState\(false\)" | grep -i "showHelp"` — expect one match for the showHelp state
    9. `git diff --stat frontend/src/styles/global.css` — expect a +30/-0 (or near it) addition only, no other changes

    If any verification fails, STOP and surface the discrepancy to the user with exact output. Do NOT attempt to "fix" the working tree — these files were authored by the user and the planner's job is to commit them as-is (D-01 + PATTERNS.md "Commit as-is").
  </action>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `claude/resume-ai-app-ClIA9`
    - `git status --porcelain frontend/src/pages/Help.tsx` outputs `?? frontend/src/pages/Help.tsx`
    - `git status --porcelain frontend/src/App.tsx` starts with ` M ` (space-M)
    - `git status --porcelain frontend/src/styles/global.css` starts with ` M ` (space-M)
    - `grep -c "export default function Help" frontend/src/pages/Help.tsx` returns `1`
    - `git diff frontend/src/App.tsx | grep -c "^+import Help from"` returns `1`
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -Fxq "claude/resume-ai-app-ClIA9" && git status --porcelain frontend/src/pages/Help.tsx | grep -Fxq "?? frontend/src/pages/Help.tsx" && grep -c "export default function Help" frontend/src/pages/Help.tsx | grep -Fxq "1"</automated>
  </verify>
  <done>All 9 verification checks pass without discrepancy; working tree is in the exact pre-staging state PATTERNS.md describes.</done>
</task>

<task type="auto">
  <name>Task 2: Stage exactly the three Help-modal files and create the atomic commit</name>
  <files>frontend/src/pages/Help.tsx, frontend/src/App.tsx, frontend/src/styles/global.css</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (Commit 1 section + "Conventional Commits Style" lines 58-83)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-14 commit 1 subject is locked verbatim)
  </read_first>
  <action>
    From the repo root, run exactly these commands in order:

    1. Stage the three files explicitly by name (no `git add .`, no `-A`, per `<critical_rules>` of GSD planner — avoid accidentally including other untracked files):
       ```
       git add frontend/src/pages/Help.tsx frontend/src/App.tsx frontend/src/styles/global.css
       ```

    2. Verify staging is correct (must show exactly 3 files staged, nothing else):
       ```
       git diff --cached --name-status
       ```
       Expected output (3 lines):
       ```
       A	frontend/src/pages/Help.tsx
       M	frontend/src/App.tsx
       M	frontend/src/styles/global.css
       ```
       If output has more or fewer lines, STOP and surface — do not commit.

    3. Create the commit with the verbatim subject from D-14 commit 1:
       ```
       git commit -m "feat(help): in-app Help modal with how-to-play guidance"
       ```
       Subject is **lowercase**, type prefix `feat`, scope `help`, no trailing period — matches Conventional Commits style verified in `git log --oneline -20` (PATTERNS.md lines 60-72). No body needed for this commit.

    4. Verify the commit landed correctly:
       ```
       git log -1 --format=%s
       ```
       Must output exactly: `feat(help): in-app Help modal with how-to-play guidance`

    5. Verify file count:
       ```
       git show HEAD --name-status
       ```
       Must list exactly 3 entries: A frontend/src/pages/Help.tsx, M frontend/src/App.tsx, M frontend/src/styles/global.css.

    Do NOT push to origin — D-15 says push after the **whole phase** verifies, not after individual commits.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` outputs exactly `feat(help): in-app Help modal with how-to-play guidance`
    - `git show HEAD --name-status | grep -c "^A.*frontend/src/pages/Help\.tsx$"` returns `1`
    - `git show HEAD --name-status | grep -c "^M.*frontend/src/App\.tsx$"` returns `1`
    - `git show HEAD --name-status | grep -c "^M.*frontend/src/styles/global\.css$"` returns `1`
    - `git show HEAD --name-status | grep -cE "^[AMD]"` returns `3` (exactly three file changes — no extras)
    - `git status --porcelain frontend/src/pages/Help.tsx frontend/src/App.tsx frontend/src/styles/global.css` outputs nothing (all three now committed)
    - `git rev-parse --abbrev-ref HEAD` still outputs `claude/resume-ai-app-ClIA9` (no branch switch occurred)
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --format=%s | grep -Fxq "feat(help): in-app Help modal with how-to-play guidance" && [ "$(git show HEAD --name-status | grep -cE '^[AMD]')" = "3" ] && [ -z "$(git status --porcelain frontend/src/pages/Help.tsx frontend/src/App.tsx frontend/src/styles/global.css)" ]</automated>
  </verify>
  <done>Commit `feat(help):` exists at HEAD on branch `claude/resume-ai-app-ClIA9` with exactly 3 file entries; working tree no longer shows any of the 3 Help-modal files as untracked or modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working-tree → git index | User-authored frontend code crosses into the version-controlled record |
| local repo → origin (deferred to phase end) | Phase 1 stays local until D-15 final push, so this boundary is not crossed by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | git stage → commit | mitigate | Stage three files by explicit name (`git add <file> <file> <file>`), never `git add .` or `-A`; PATTERNS.md confirms `.env`, `bingo.db`, `frontend/bingo.db` are gitignored so `git add .` would NOT pick them up — but explicit naming is defense-in-depth and aligns with GSD `<critical_rules>` |
| T-01-02 | Tampering | wrong commit subject leaks into history (e.g. typo, capitalised) | mitigate | Subject string is verbatim-locked from D-14; verification grep `git log -1 --format=%s` exact match is part of acceptance criteria; if subject is wrong the commit can be amended via `git commit --amend` since it has not been pushed (D-15 defers push to phase end) |
| T-01-03 | Repudiation | branch switch without intent loses the commit | mitigate | Task 1 verifies HEAD is `claude/resume-ai-app-ClIA9` before staging; Task 2 re-verifies after commit; refuse to switch branches under any circumstance |
</threat_model>

<verification>
After Task 2 completes, the following must all hold:

```bash
# Branch is correct (D-15)
git rev-parse --abbrev-ref HEAD
# → claude/resume-ai-app-ClIA9

# Commit subject matches D-14 verbatim
git log -1 --format=%s
# → feat(help): in-app Help modal with how-to-play guidance

# Exactly 3 files in commit (no spillover)
git show HEAD --name-status | grep -cE "^[AMD]"
# → 3

# All three target files are now in the committed state
git status --porcelain frontend/src/pages/Help.tsx frontend/src/App.tsx frontend/src/styles/global.css
# → (empty output)

# No accidental commit of secrets / DB files
git show HEAD --name-status | grep -iE "\.env|bingo\.db"
# → (empty output)
```
</verification>

<success_criteria>
- The Help modal commit (`feat(help): ...`) is HEAD on `claude/resume-ai-app-ClIA9`
- Exactly three files are part of that commit
- No `.env`, `bingo.db`, or other unintended file is part of the commit
- Working tree is clean for the three Help-modal files (they are no longer untracked or modified)
- Branch was not switched, no force-push, no rebase, no history rewrite (per CLAUDE.md hard constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-hygiene/01-01-SUMMARY.md` capturing:
- Commit hash + subject of the new commit
- File count and names
- Confirmation the working tree is clean for these three files
- Any deviation from PATTERNS.md (expect: none)
</output>
