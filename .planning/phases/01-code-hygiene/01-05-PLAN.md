---
phase: 01-code-hygiene
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - .gitignore
  - frontend/bingo.db
autonomous: true
requirements: [POLISH-03]
tags: [git-hygiene, atomic-commit, infra, gitignore, idempotent-untrack]

must_haves:
  truths:
    - "An atomic commit with subject `chore(repo): untrack .env and bingo.db*, tighten .gitignore` exists at HEAD on branch claude/resume-ai-app-ClIA9"
    - "The commit contains exactly 1 git-tracked file change: .gitignore (modified)"
    - "frontend/bingo.db file no longer exists in the working tree (gitignored, never tracked — deleted with plain rm)"
    - "The root bingo.db, bingo.db-wal, bingo.db-shm files (if present in working tree) are preserved — D-10 lock"
    - "After this commit, `git ls-files | grep -E '^(\\.env|bingo\\.db)'` returns nothing (neither tracked, regardless of whether anything was actually untracked by the rm step)"
    - "The phase has 5 commits in correct D-14 order; working tree is clean for all phase-touched files"
  artifacts:
    - path: ".gitignore"
      provides: "Tightened git-ignore rules — bingo.db wildcard catches WAL/SHM, comments document intent"
      contains: "bingo.db*"
      must_not_contain_lines: ["^bingo.db-wal$", "^bingo.db-shm$"]
    - path: "frontend/bingo.db"
      provides: "Deleted file — must NOT exist after this commit"
      must_not_exist: true
    - path: "bingo.db"
      provides: "Preserved real DB at repo root (Mum & Gordie's prior game data, D-10)"
      must_exist: true
  key_links:
    - from: ".gitignore"
      to: "git's ignore engine"
      via: "single bingo.db* wildcard line replaces three specific filenames"
      pattern: "^bingo\\.db\\*"
---

<objective>
Final phase commit — POLISH-03 repo hygiene. Three concrete actions in one atomic commit:
1. Run `git rm --cached --ignore-unmatch .env bingo.db bingo.db-wal bingo.db-shm` (idempotent — pattern mapper verified these are NOT currently tracked, so this is a no-op in practice but the command is run anyway to make the commit's intent self-documenting; `--ignore-unmatch` prevents the failure mode)
2. Replace three specific filenames in `.gitignore` (`bingo.db`, `bingo.db-wal`, `bingo.db-shm`) with one wildcard `bingo.db*` plus inline comments per D-12
3. Delete `frontend/bingo.db` (plain `rm` — file is gitignored, was never tracked, but is a 0-byte accidental dupe per D-11)

The verbatim D-10 from CONTEXT.md says `git rm --cached .env bingo.db bingo.db-wal bingo.db-shm` — but the pattern mapper's pre-flight verification confirmed these files are NOT in the git index, so that command would fail with `fatal: pathspec '...' did not match any files`. Per the **load-bearing correction** at the top of planning_context, the executor MUST use `--ignore-unmatch` to keep the operation idempotent. The acceptance criterion shifts from "files removed from index" (impossible — they were never there) to "files are not in index AND .gitignore wildcard prevents future re-add".

Purpose: POLISH-03 — make secrets and local DB files ungit-trackable going forward. CLAUDE.md hard constraint forbids history rewrite, so this is "clean going forward" hygiene only. Repo stays private; v2's `PUBLIC-01` will handle history scrubbing if/when public release is on the table.

Output: One git commit on `claude/resume-ai-app-ClIA9` containing exactly the .gitignore modification. The `frontend/bingo.db` deletion does NOT appear in the commit because the file was never tracked — it's a working-tree-only side effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-hygiene/01-CONTEXT.md
@.planning/phases/01-code-hygiene/01-PATTERNS.md
@.planning/phases/01-code-hygiene/01-04-SUMMARY.md

<interfaces>
<!-- .gitignore — current state (verbatim, 14 lines from PATTERNS.md) -->

```
__pycache__/
*.pyc
*.pyo
venv/
.env
bingo.db
bingo.db-wal
bingo.db-shm
node_modules/
frontend/dist/
*.log
.DS_Store
.claude/
```

<!-- .gitignore — TARGET state (13 lines: 3 specific bingo lines collapse to 1 wildcard with comment) -->

```
__pycache__/
*.pyc
*.pyo
venv/
.env                  # secrets — never commit
bingo.db*             # SQLite DB + WAL/SHM — local data, never commit
node_modules/
frontend/dist/
*.log
.DS_Store
.claude/
```

<!-- The idempotent untrack command (load-bearing correction to D-10) -->

```bash
git rm --cached --ignore-unmatch .env bingo.db bingo.db-wal bingo.db-shm
```

<!-- The frontend/bingo.db deletion (plain rm, file is gitignored not tracked, per D-11) -->

```bash
rm frontend/bingo.db
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run defensive idempotent untrack and verify state</name>
  <files>.env, bingo.db, bingo.db-wal, bingo.db-shm</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (commit 5 section, lines 595-684, especially "Pre-flight verification" lines 604-617 and "Important pre-flight finding" at top, lines 9-22)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-10, D-11, D-12, D-13)
    - .gitignore (current 14-line state — confirm lines 6-8 are the three specific bingo filenames)
  </read_first>
  <action>
    From repo root:

    1. Branch + prior-commit check:
       ```
       git rev-parse --abbrev-ref HEAD     # → claude/resume-ai-app-ClIA9
       git log -1 --format=%s              # → chore(telegram): remove bot integration end-to-end
       ```

    2. Pre-flight verification (per PATTERNS.md lines 605-611) — these commands document the current state of the index:
       ```
       git ls-files | grep -iE "bingo\.db|^\.env$"
       # expected: prints only `.env.example` (or possibly nothing) — should NOT print `.env`, `bingo.db`, `bingo.db-wal`, `bingo.db-shm`, or `frontend/bingo.db`

       git log --all --full-history -- .env bingo.db bingo.db-wal bingo.db-shm frontend/bingo.db
       # expected: no output (no commits ever touched these paths — pattern mapper verified 2026-05-04)
       ```
       Record the outputs in this task's notes for the SUMMARY. If the pre-flight unexpectedly shows any of these files in `git ls-files`, then the audit was right after all and the command in step 3 will actually do something — proceed normally; the acceptance criterion still works because it's framed as "after this task, none of these paths are tracked".

    3. Run the **defensive idempotent untrack** (load-bearing correction):
       ```
       git rm --cached --ignore-unmatch .env bingo.db bingo.db-wal bingo.db-shm
       ```
       The `--ignore-unmatch` flag is critical — without it, this command exits non-zero with `fatal: pathspec '...' did not match any files` because pattern mapper verified these files are not in the index as of 2026-05-04. With the flag, it's a clean no-op (or, in the unlikely event the audit was right and they ARE tracked, it does the actual untrack). Either way it's safe.

       Record the command output. Empty output = the files weren't tracked (matches pattern mapper finding). Non-empty output = the files were tracked and `git rm` reported them as removed from the index.

    4. Post-state verification:
       ```
       git ls-files | grep -E '^(\.env|bingo\.db)'
       # expected: empty (regardless of whether step 3 actually did anything, the END state must be: NONE of these tracked)

       git status --porcelain .env bingo.db bingo.db-wal bingo.db-shm 2>/dev/null
       # expected: either empty (gitignored, untracked) OR shows them as untracked. Either is fine. They must NOT be staged (no `D ` or `A ` prefix).
       ```

    5. Verify the real DB is preserved per D-10:
       ```
       test -f bingo.db && echo "REAL_DB_PRESERVED" || echo "MISSING_REAL_DB"
       # If MISSING_REAL_DB and the file existed before this task, STOP — D-10 lock has been violated. The real DB at repo root must remain in the working tree (Mum & Gordie's game data).
       ```
       Note: if `bingo.db` did not exist before this task started (e.g. fresh checkout, no game data yet), then "MISSING_REAL_DB" is acceptable. The lock is "don't delete it if it's there" — not "create it if absent".
  </action>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `claude/resume-ai-app-ClIA9`
    - `git log -1 --format=%s` outputs `chore(telegram): remove bot integration end-to-end` (plan 04 is HEAD)
    - The `git rm --cached --ignore-unmatch` command exits with status 0 (the `--ignore-unmatch` flag guarantees this even if the files are not in the index)
    - `git ls-files | grep -cE '^(\.env|bingo\.db)'` returns `0` (nothing tracked matching .env or bingo.db prefix — note: `.env.example` does NOT match because it has the `.example` suffix; the regex is anchored)
    - `git ls-files | grep -c '^\.env$'` returns `0` (specifically `.env` is not tracked)
    - If `bingo.db` existed in the working tree before Task 1 ran, it still exists after (D-10 lock — preserve real game data)
    - The .env file in the working tree still has its `ANTHROPIC_API_KEY=...` line intact (untouched by this task — that's plan 04's territory)
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -Fxq "claude/resume-ai-app-ClIA9" && git log -1 --format=%s | grep -Fxq "chore(telegram): remove bot integration end-to-end" && git rm --cached --ignore-unmatch .env bingo.db bingo.db-wal bingo.db-shm > /dev/null 2>&1 && [ "$(git ls-files | grep -cE '^(\.env|bingo\.db)')" = "0" ] && [ "$(git ls-files | grep -c '^\.env$')" = "0" ]</automated>
  </verify>
  <done>The four hygiene-target paths are confirmed absent from the git index; `bingo.db` (if present) is preserved in working tree; the rm-cached command was idempotent and successful.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten .gitignore (3 specific lines → 1 wildcard with comments) and delete frontend/bingo.db</name>
  <files>.gitignore, frontend/bingo.db</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (commit 5 ".gitignore — EDIT" section lines 619-655 and "frontend/bingo.db" section lines 657-663)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-11, D-12)
    - .gitignore (verify current state — 14 lines, with `bingo.db`, `bingo.db-wal`, `bingo.db-shm` on lines 6-7-8)
    - frontend/bingo.db (verify it exists in the working tree and is 0 bytes per pattern mapper verification 2026-05-04)
  </read_first>
  <action>
    **Part A — Edit `.gitignore`:**

    1. Pre-flight: confirm current line 5-8 state:
       ```
       sed -n '5,8p' .gitignore
       ```
       Expected output (verbatim):
       ```
       .env
       bingo.db
       bingo.db-wal
       bingo.db-shm
       ```

    2. Apply the edit using the Edit tool. Replace the four-line block (lines 5-8) with two lines (.env with comment, bingo.db* wildcard with comment), per D-12 ("add brief inline comments"):
       - old_string (4 lines):
         ```
         .env
         bingo.db
         bingo.db-wal
         bingo.db-shm
         ```
       - new_string (2 lines):
         ```
         .env                  # secrets — never commit
         bingo.db*             # SQLite DB + WAL/SHM — local data, never commit
         ```

    3. Verify the edit:
       ```
       wc -l .gitignore                          # → 12 (down from 14: 4 lines collapsed to 2)
       grep -c "^bingo\.db\*" .gitignore         # → 1
       grep -c "^bingo\.db-wal$" .gitignore      # → 0
       grep -c "^bingo\.db-shm$" .gitignore      # → 0
       grep -c "^bingo\.db$" .gitignore          # → 0 (specific filename gone, replaced by wildcard)
       grep -c "^\.env" .gitignore               # → 1 (the .env line, possibly with trailing comment)
       grep "secrets — never commit" .gitignore  # → matches the .env comment
       ```

       Verify the wildcard is functional:
       ```
       git check-ignore -v bingo.db        # → expect a match line referencing .gitignore line for bingo.db*
       git check-ignore -v bingo.db-wal    # → match (caught by wildcard)
       git check-ignore -v bingo.db-shm    # → match (caught by wildcard)
       git check-ignore -v .env            # → match (still ignored)
       ```

    **Part B — Delete `frontend/bingo.db` (D-11):**

    1. Pre-flight: confirm it exists and is gitignored (per pattern mapper, it's a 0-byte accidental dupe):
       ```
       ls -la frontend/bingo.db   # → expect a 0-byte file
       git ls-files frontend/bingo.db  # → empty (not tracked)
       git check-ignore -v frontend/bingo.db  # → match (covered by frontend/dist/ rule? No — covered by the new bingo.db* wildcard which is repo-relative, but the path component "frontend/" means it needs the ** semantics. Verify: gitignore patterns without leading `/` match in any directory. So `bingo.db*` matches `frontend/bingo.db`. Good.)
       ```

       If the file does NOT exist, skip the rm — it's already gone. Note this in the SUMMARY.

    2. Delete with plain `rm` (NOT `git rm` — file is not tracked, never was):
       ```
       rm frontend/bingo.db
       ```

    3. Post-check:
       ```
       test ! -f frontend/bingo.db && echo "OK_DELETED" || echo "FAILED"
       ```
       Must print `OK_DELETED`.

    Do NOT stage or commit yet — Task 3 stages and commits everything atomically.
  </action>
  <acceptance_criteria>
    - `wc -l < .gitignore` returns `12` (down from 14)
    - `grep -c "^bingo\.db\*" .gitignore` returns `1`
    - `grep -c "^bingo\.db$" .gitignore` returns `0` (specific filename collapsed into wildcard)
    - `grep -c "^bingo\.db-wal$" .gitignore` returns `0`
    - `grep -c "^bingo\.db-shm$" .gitignore` returns `0`
    - `git check-ignore -v bingo.db-wal` exits 0 (wildcard catches WAL files)
    - `git check-ignore -v bingo.db-shm` exits 0 (wildcard catches SHM files)
    - `git check-ignore -v .env` exits 0 (still ignored)
    - `test ! -f frontend/bingo.db` is true (file deleted from working tree)
    - `git ls-files frontend/bingo.db` returns nothing (was never tracked, still isn't)
    - `git status --porcelain .gitignore` starts with ` M ` (modified, not yet staged)
  </acceptance_criteria>
  <verify>
    <automated>[ "$(wc -l < .gitignore)" = "12" ] && [ "$(grep -c '^bingo\.db\*' .gitignore)" = "1" ] && [ "$(grep -c '^bingo\.db$' .gitignore)" = "0" ] && git check-ignore -v bingo.db-wal > /dev/null && git check-ignore -v .env > /dev/null && test ! -f frontend/bingo.db</automated>
  </verify>
  <done>.gitignore has the wildcard rule with comments; specific filename rules are gone; frontend/bingo.db is deleted from working tree; gitignore wildcard correctly catches all three SQLite file variants.</done>
</task>

<task type="auto">
  <name>Task 3: Stage .gitignore and create the final phase commit</name>
  <files>.gitignore</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (commit 5 acceptance criteria lines 665-674)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-13, D-14 commit 5 subject is verbatim-locked, D-15)
  </read_first>
  <action>
    From repo root:

    1. Stage exactly `.gitignore`. Note: `frontend/bingo.db` deletion does NOT need staging (file was never tracked, so its absence is a working-tree-only change). The `git rm --cached` from Task 1 either staged something (unlikely per pattern mapper) or did nothing — either way, `git diff --cached --name-status` after this stage step will show what's actually about to be committed.

       ```
       git add .gitignore
       ```

    2. Verify staging:
       ```
       git diff --cached --name-status
       ```
       Expected output: ONE line, `M	.gitignore`. If output also shows deletion entries for `.env`, `bingo.db`, etc., that means Task 1's `git rm --cached` actually did remove them from the index — which is also correct and acceptable (matches the literal CONTEXT.md D-10 intent). The commit can include both `.gitignore` modification AND any deletion entries from Task 1.

       If output shows ANY OTHER file (e.g. an unrelated working-tree change), STOP and `git reset HEAD <file>` to unstage. Then re-verify.

    3. Per D-13, the `.env.example` comment header is **optional** and pattern mapper recommended NOT including it ("Do not include this in commit 5 if it adds churn; keep that commit focused on the gitignore/repo-state change"). Skip the .env.example edit. Acceptance criteria do NOT require it.

    4. Create the commit with the verbatim D-14 subject:
       ```
       git commit -m "chore(repo): untrack .env and bingo.db*, tighten .gitignore"
       ```

    5. Verify:
       ```
       git log -1 --format=%s   # → chore(repo): untrack .env and bingo.db*, tighten .gitignore
       git show HEAD --stat
       # → expect at least .gitignore | (some line count change). If pattern mapper was right, the commit has just .gitignore. If audit was right, it also has D entries for .env / bingo.db / bingo.db-wal / bingo.db-shm.
       ```

    6. Final phase verification — all 5 commits in order:
       ```
       git log --oneline -5 --format=%s
       ```
       Expected (most recent first):
       ```
       chore(repo): untrack .env and bingo.db*, tighten .gitignore
       chore(telegram): remove bot integration end-to-end
       fix(moments): proper logging in moment generation router
       feat(youtube): graceful transcript fallback + logging
       feat(help): in-app Help modal with how-to-play guidance
       ```

    7. Working-tree clean check for all phase-touched paths:
       ```
       git status --porcelain
       # → expect empty (or only show path .env which is gitignored — confirm with: git status --porcelain | grep -v "^\?\? \.env$")
       ```
       The phase has not modified anything else; the working tree should be clean (modulo the gitignored `.env` working file).

    8. **D-15 push step** (per phase boundary lock — push only after the whole phase verifies):
       ```
       git push origin claude/resume-ai-app-ClIA9
       ```
       This pushes all 5 phase commits to origin. Per CLAUDE.md hard constraints: repo is private, no public release, kainga-core only. Push is safe — branch is the existing one (D-15: "Stay on existing branch — no new phase branch").

       If the push fails (e.g. network issue, auth required), STOP and surface to user — do NOT force-push, do NOT create a new branch. The user can retry the push manually if needed; the local branch state is the source of truth and re-pushable.

    Do NOT amend, do NOT rebase, do NOT force-push. CLAUDE.md hard constraint: "No history rewrite of the existing git repo".
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` outputs exactly `chore(repo): untrack .env and bingo.db*, tighten .gitignore`
    - `git show HEAD --name-status | grep -c "^M.*\.gitignore$"` returns `1`
    - `git log --oneline -5 --format=%s` outputs the 5 phase commits in correct order (most recent first as listed in step 6 above)
    - `git status --porcelain | grep -v "^\?\? \.env$"` returns nothing (working tree is clean except for gitignored .env)
    - `git ls-files | grep -E '^(\.env|bingo\.db|frontend/bingo\.db)'` returns nothing (no .env or any bingo.db variant tracked at any level)
    - `git ls-files .env.example` returns one line (`.env.example` IS still tracked — D-13 lock, untouched)
    - `test -f .gitignore && grep -c "^bingo\.db\*" .gitignore` returns `1` (wildcard rule in place)
    - The remote `origin/claude/resume-ai-app-ClIA9` has all 5 phase commits after the push step (verifiable with `git rev-parse origin/claude/resume-ai-app-ClIA9 == git rev-parse HEAD`)
    - No force-push occurred: `git reflog claude/resume-ai-app-ClIA9 | head -5` does not contain "forced-update" entries from this session
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --format=%s | grep -Fxq "chore(repo): untrack .env and bingo.db*, tighten .gitignore" && git show HEAD --name-status | grep -q "^M.*\.gitignore$" && [ "$(git ls-files | grep -cE '^(\.env|bingo\.db|frontend/bingo\.db)')" = "0" ] && [ "$(git ls-files .env.example | wc -l)" = "1" ]</automated>
  </verify>
  <done>Final phase commit `chore(repo):` is HEAD; .gitignore has the wildcard; no .env/bingo.db variant is tracked; .env.example is preserved; all 5 phase commits are pushed to origin on `claude/resume-ai-app-ClIA9`; phase is shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working-tree → git index | The .gitignore edit and any deletion entries from `git rm --cached` cross into version-controlled record |
| local repo → origin (Phase boundary push per D-15) | All 5 phase commits cross to the private remote at the end of Task 3; CLAUDE.md confirms repo stays private |
| `.env` working-tree → (never crosses to git index again) | New wildcard + existing rule guarantees future `git add .env` is silently ignored |
| Real `bingo.db` (Mum & Gordie's game data) → preserved on disk | D-10 lock — the file is preserved in working tree but never tracked; new wildcard catches it for future protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | future accidental `git add .env` slipping ANTHROPIC_API_KEY into a commit | mitigate | New `.env` line in .gitignore (with explanatory comment) and existing `.env.example`-only tracking are defense-in-depth; the wildcard `bingo.db*` similarly prevents the WAL/SHM files from sneaking in. Future `git status` will not show .env as untracked-and-dirty, so `git add -A` (which the planner forbids in `<critical_rules>`) would still skip it. |
| T-05-02 | Information Disclosure | accidental loss of real `bingo.db` (Mum & Gordie's game data) | mitigate | D-10 explicitly says working-tree DB stays put. `git rm --cached --ignore-unmatch` does NOT touch the working-tree file (only the index). Acceptance criterion verifies `bingo.db` (if it existed pre-task) still exists post-task. The frontend/bingo.db dupe deleted in Task 2 is a 0-byte accident, not real data (pattern mapper verified 2026-05-04). |
| T-05-03 | Tampering | the `git rm --cached` command failing without `--ignore-unmatch` would abort the commit and leave the phase incomplete | mitigate | Pattern mapper's load-bearing correction is encoded as the literal command in Task 1. `--ignore-unmatch` makes the operation idempotent regardless of git state. Acceptance criterion checks that the END state matches the goal, not that any specific intermediate operation did anything. |
| T-05-04 | Repudiation | force-push or rebase mangles phase history | mitigate | CLAUDE.md hard constraint forbids history rewrite; Task 3 step 8 explicitly says "do NOT force-push"; acceptance criterion checks `git reflog` for "forced-update" entries; the push command is plain `git push origin <branch>` with no `-f`/`--force` flags. |
| T-05-05 | Information Disclosure | pushing the phase to origin exposes the working branch publicly | accept | CLAUDE.md confirms repo is **private** and "kainga-core only. No public IP, no public release". The remote is the private GitHub-hosted repo (or equivalent); D-15 explicitly authorises the push as a phase-boundary action. Public exposure risk is negligible per the locked privacy posture. |
| T-05-06 | Denial of Service | network failure during the final push leaves the phase locally-committed but not pushed | accept | Local commits are intact and re-pushable. CLAUDE.md context says "Branch in flight... 1 commit ahead of origin before this session" — repo is already accustomed to local-ahead-of-origin state. User can retry the push manually. Not a phase-failure condition. |
</threat_model>

<verification>
After Task 3:

```bash
# Branch + final commit
git rev-parse --abbrev-ref HEAD                                # → claude/resume-ai-app-ClIA9
git log -1 --format=%s                                          # → chore(repo): untrack .env and bingo.db*, tighten .gitignore

# All 5 phase commits in order (most recent first)
git log --oneline -5 --format=%s
# expect:
# chore(repo): untrack .env and bingo.db*, tighten .gitignore
# chore(telegram): remove bot integration end-to-end
# fix(moments): proper logging in moment generation router
# feat(youtube): graceful transcript fallback + logging
# feat(help): in-app Help modal with how-to-play guidance

# Repo hygiene proven
git ls-files | grep -E '^(\.env|bingo\.db|frontend/bingo\.db)'   # → empty
git ls-files .env.example                                         # → .env.example
grep -c "^bingo\.db\*" .gitignore                                 # → 1
grep -c "^bingo\.db$" .gitignore                                  # → 0
test ! -f frontend/bingo.db && echo OK                            # → OK
test -f bingo.db && echo "REAL_DB_PRESERVED" || echo "FRESH_TREE" # → REAL_DB_PRESERVED if Mum&Gordie data was there pre-phase

# .env still has its real key, no Telegram lines (set up by plan 04 and confirmed here)
grep -c "^ANTHROPIC_API_KEY=" .env                                # → 1
grep -cE "^(TELEGRAM_|BASE_URL=)" .env                            # → 0

# Working tree is clean (modulo gitignored .env which appears as untracked-but-ignored, not in porcelain by default)
git status --porcelain                                            # → empty

# Push succeeded — origin matches HEAD
[ "$(git rev-parse HEAD)" = "$(git rev-parse origin/claude/resume-ai-app-ClIA9 2>/dev/null)" ] && echo PUSHED_OK
# → PUSHED_OK

# No history rewrite — branch reflog is forward-only
git reflog claude/resume-ai-app-ClIA9 | grep -c "forced-update"  # → 0
```
</verification>

<success_criteria>
- Commit `chore(repo): untrack .env and bingo.db*, tighten .gitignore` is HEAD on `claude/resume-ai-app-ClIA9`
- All 5 phase commits exist in correct D-14 order (verifiable via `git log --oneline -5`)
- `.gitignore` has the `bingo.db*` wildcard with inline comments
- No .env, bingo.db, bingo.db-wal, bingo.db-shm, or frontend/bingo.db is tracked by git at any level
- `.env.example` is preserved untouched (D-13 lock — Phase 2 will update it for AI provider vars)
- `frontend/bingo.db` is deleted from the working tree
- `bingo.db` at repo root is preserved (D-10 lock for Mum & Gordie's game data)
- Branch was pushed to origin without force, without rebase, without amending — CLAUDE.md "no history rewrite" hard constraint upheld
- POLISH-03 requirement is fully addressed by this single plan
- Phase 1 — Code Hygiene — is shippable
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-hygiene/01-05-SUMMARY.md` capturing:
- Commit hash + subject
- Whether the `git rm --cached --ignore-unmatch` actually untracked anything (it should NOT have, per pattern mapper, but record actual behaviour for future audits)
- Final phase commit list (5 commits, hashes + subjects)
- Confirmation .env.example was NOT touched (D-13 deferred to Phase 2)
- Confirmation push to origin succeeded
- Reminder for the user about the deferred @BotFather revocation (carried over from plan 04)
- Phase 1 status: COMPLETE — all of POLISH-01, POLISH-02, POLISH-03 delivered
</output>
