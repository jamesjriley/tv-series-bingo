---
phase: 01-code-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/services/youtube_service.py
autonomous: true
requirements: [POLISH-01]
tags: [git-hygiene, atomic-commit, backend, logging, youtube]

must_haves:
  truths:
    - "An atomic commit with subject `feat(youtube): graceful transcript fallback + logging` exists at HEAD on branch claude/resume-ai-app-ClIA9"
    - "The commit contains exactly 1 file: backend/services/youtube_service.py (modified)"
    - "youtube_service.py imports `logging` and defines `logger = logging.getLogger(__name__)` at module scope"
    - "The previously silent `except Exception:` in fetch_transcripts now logs a warning with the video_id and exception type"
  artifacts:
    - path: "backend/services/youtube_service.py"
      provides: "Module-scope logger and warning-level log on transcript fetch failure"
      contains: "logger = logging.getLogger(__name__)"
  key_links:
    - from: "backend/services/youtube_service.py"
      to: "Python stdlib logging"
      via: "import logging + logger = logging.getLogger(__name__)"
      pattern: "logger = logging.getLogger\\(__name__\\)"
---

<objective>
Land the in-flight YouTube-transcript-fallback + logging changes as commit 2 of 5 (D-14). Like commit 1, the work is already authored — the diff is sitting unstaged in `backend/services/youtube_service.py`. The job is to verify the diff matches PATTERNS.md, stage exactly that one file, and commit with the locked subject.

Purpose: POLISH-01b — the existing `except Exception: pass` in `fetch_transcripts` was a silent-fail flagged by CONCERNS.md. The working-tree diff replaces it with `logger.warning(...)` and establishes the **canonical local-logger pattern** for the project (mirrored by commit 3's moments.py change).
Output: One git commit on branch `claude/resume-ai-app-ClIA9` containing exactly one file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-hygiene/01-CONTEXT.md
@.planning/phases/01-code-hygiene/01-PATTERNS.md
@.planning/phases/01-code-hygiene/01-01-SUMMARY.md

<interfaces>
<!-- Canonical logger pattern this commit establishes (PATTERNS.md "Shared / Cross-Cutting Patterns") -->

```python
import logging
...
logger = logging.getLogger(__name__)
```

<!-- Then used as -->

```python
logger.warning("message %s", value)   # lazy %-formatting, NOT f-strings
logger.exception("message")           # inside except blocks; auto-captures traceback
```

<!-- Verbatim diff that must already be in the working tree (PATTERNS.md lines 200-221) -->

```diff
@@ -1,10 +1,13 @@
 import json
+import logging
 import re
 import xml.etree.ElementTree as ET

 import httpx
 from youtube_transcript_api import YouTubeTranscriptApi

+logger = logging.getLogger(__name__)
+
 _HEADERS = {
@@ -36,8 +39,8 @@ def fetch_transcripts(video_urls: list[str]) -> str:
             transcript = ytt.fetch(video_id)
             text = " ".join(snippet.text for snippet in transcript)
             all_text.append(f"--- Video {video_id} ---\n{text}")
-        except Exception:
-            # Skip videos without available transcripts
+        except Exception as e:
+            logger.warning("Transcript unavailable for %s: %s", video_id, type(e).__name__)
             continue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the working-tree diff matches the verbatim PATTERNS.md hunk</name>
  <files>backend/services/youtube_service.py</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (Commit 2 section, lines 188-240)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-14 commit 2 subject + D-01)
    - backend/services/youtube_service.py (working-tree state — confirm logger at line 9, warning at line ~43)
  </read_first>
  <action>
    From repo root:

    1. Confirm branch (D-15): `git rev-parse --abbrev-ref HEAD` → `claude/resume-ai-app-ClIA9`
    2. Confirm prior commit landed: `git log -1 --format=%s` → `feat(help): in-app Help modal with how-to-play guidance` (commit 1 must already be HEAD before this plan runs — depends_on enforces wave order)
    3. Confirm youtube_service.py is the only POLISH-01b modified file: `git status --porcelain backend/services/youtube_service.py` → ` M backend/services/youtube_service.py`
    4. Verify the new logger line exists at line 9 of the working-tree file:
       ```
       sed -n '9p' backend/services/youtube_service.py
       ```
       Must equal exactly: `logger = logging.getLogger(__name__)`
    5. Verify the warning line was added (line ~43 in new file):
       ```
       grep -n 'logger.warning("Transcript unavailable for %s: %s"' backend/services/youtube_service.py
       ```
       Must return one match (line number around 43, exact line varies; the pattern presence is what matters).
    6. Verify the silent `except Exception: pass`-style block has been removed:
       ```
       grep -nA1 "except Exception:" backend/services/youtube_service.py
       ```
       The match must be `except Exception as e:` followed by the `logger.warning` line — NOT `except Exception:` followed by `# Skip videos without available transcripts`. If the old pattern is still there, the working-tree diff has not been written — STOP and surface to the user.
    7. Confirm `import logging` is present in import block:
       ```
       grep -nE "^import logging$" backend/services/youtube_service.py
       ```
       Must return one match (line 2 per PATTERNS.md).

    If any check fails, STOP — do NOT modify the file. The pattern mapper recorded the diff verbatim from the working tree on 2026-05-04; if it's gone, something else has happened that the user needs to know about.
  </action>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `claude/resume-ai-app-ClIA9`
    - `git log -1 --format=%s` outputs `feat(help): in-app Help modal with how-to-play guidance` (prior plan landed)
    - `git status --porcelain backend/services/youtube_service.py` starts with ` M `
    - `sed -n '9p' backend/services/youtube_service.py` outputs exactly `logger = logging.getLogger(__name__)`
    - `grep -c '^import logging$' backend/services/youtube_service.py` returns `1`
    - `grep -c 'logger.warning("Transcript unavailable for %s: %s"' backend/services/youtube_service.py` returns `1`
    - `grep -c "except Exception as e:" backend/services/youtube_service.py` returns `1` (the new form, with `as e`)
  </acceptance_criteria>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -Fxq "claude/resume-ai-app-ClIA9" && git log -1 --format=%s | grep -Fxq "feat(help): in-app Help modal with how-to-play guidance" && [ "$(sed -n '9p' backend/services/youtube_service.py)" = "logger = logging.getLogger(__name__)" ] && [ "$(grep -c 'logger.warning(\"Transcript unavailable for %s: %s\"' backend/services/youtube_service.py)" = "1" ]</automated>
  </verify>
  <done>Working-tree diff in `backend/services/youtube_service.py` matches the PATTERNS.md verbatim hunk; commit 1 is confirmed at HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Stage backend/services/youtube_service.py and create the commit</name>
  <files>backend/services/youtube_service.py</files>
  <read_first>
    - .planning/phases/01-code-hygiene/01-PATTERNS.md (Commit 2 acceptance criteria, lines 233-240)
    - .planning/phases/01-code-hygiene/01-CONTEXT.md (D-14 commit 2 subject is verbatim-locked)
  </read_first>
  <action>
    From repo root, run exactly:

    1. Stage by explicit name (no `git add .` — defense in depth):
       ```
       git add backend/services/youtube_service.py
       ```

    2. Verify staging is exactly this one file:
       ```
       git diff --cached --name-status
       ```
       Expected: `M	backend/services/youtube_service.py` (one line). If more lines, STOP — do not commit.

    3. Commit with the verbatim D-14 subject:
       ```
       git commit -m "feat(youtube): graceful transcript fallback + logging"
       ```

    4. Verify the commit:
       ```
       git log -1 --format=%s
       ```
       Must output exactly: `feat(youtube): graceful transcript fallback + logging`

    5. Verify file count is 1:
       ```
       git show HEAD --name-status | grep -cE "^[AMD]"
       ```
       Must output `1`.

    Do NOT push to origin (D-15 — push after whole phase verifies).

    Sanity smoke (cheap; doesn't run any external network call):
    ```
    python3 -c "import importlib, sys; sys.path.insert(0, '.'); importlib.import_module('backend.services.youtube_service')"
    ```
    Must succeed with no errors. (This confirms the new `import logging` and `logger = logging.getLogger(__name__)` don't break the module's import-time semantics.)
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` outputs exactly `feat(youtube): graceful transcript fallback + logging`
    - `git show HEAD --name-status | grep -c "^M.*backend/services/youtube_service\.py$"` returns `1`
    - `git show HEAD --name-status | grep -cE "^[AMD]"` returns `1` (exactly one file)
    - `git status --porcelain backend/services/youtube_service.py` outputs nothing (committed)
    - `python3 -c "import importlib, sys; sys.path.insert(0, '.'); importlib.import_module('backend.services.youtube_service')"` exits 0
    - `grep -n "logger = logging.getLogger(__name__)" backend/services/youtube_service.py` returns line 9 (per PATTERNS.md verbatim acceptance criterion)
  </acceptance_criteria>
  <verify>
    <automated>git log -1 --format=%s | grep -Fxq "feat(youtube): graceful transcript fallback + logging" && [ "$(git show HEAD --name-status | grep -cE '^[AMD]')" = "1" ] && [ -z "$(git status --porcelain backend/services/youtube_service.py)" ] && python3 -c "import importlib, sys; sys.path.insert(0, '.'); importlib.import_module('backend.services.youtube_service')"</automated>
  </verify>
  <done>Commit `feat(youtube):` is HEAD; one file modified; module still imports cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working-tree → git index | User-authored backend logging change crosses into version-controlled record |
| logger output → stdout/log file | New `logger.warning` lines may be captured in any future log destination — must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | logger.warning at line 43 | accept | The warning logs `video_id` (already public — comes from user-supplied YouTube URL) and `type(e).__name__` (exception class name only, not message). No tokens, no API keys, no PII — explicit pattern of `type(e).__name__` over `str(e)` is part of the diff. Acceptable risk. |
| T-02-02 | Tampering | accidental staging of unrelated files | mitigate | Stage by explicit filename only; `git diff --cached --name-status` must show exactly one line before commit |
| T-02-03 | Denial of Service | future call sites that pass attacker-controlled `video_id` | accept | This commit doesn't introduce new attack surface — the silent `except` was already swallowing whatever exceptions YouTubeTranscriptApi raised; replacing `pass` with `logger.warning` strictly improves observability without changing input handling. Out of scope to harden further (v2 / SEC-01-adjacent). |
</threat_model>

<verification>
After Task 2:

```bash
# Branch lock
git rev-parse --abbrev-ref HEAD
# → claude/resume-ai-app-ClIA9

# Commit subject
git log -1 --format=%s
# → feat(youtube): graceful transcript fallback + logging

# One-file commit
git show HEAD --name-status | grep -cE "^[AMD]"
# → 1

# Logger pattern is in place
grep -n "logger = logging.getLogger(__name__)" backend/services/youtube_service.py
# → 9:logger = logging.getLogger(__name__)

# No accidental .env / bingo.db inclusion
git show HEAD --name-status | grep -iE "\.env|bingo\.db"
# → (empty)
```
</verification>

<success_criteria>
- Commit `feat(youtube): graceful transcript fallback + logging` is HEAD on `claude/resume-ai-app-ClIA9`
- Exactly one file (`backend/services/youtube_service.py`) is in the commit
- The canonical logger pattern (`logger = logging.getLogger(__name__)`) is now established in the codebase, ready for commit 3 to mirror
- Module still imports without error (no syntax / import-time regression)
- No history rewrite, no force-push (CLAUDE.md constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-hygiene/01-02-SUMMARY.md` capturing:
- Commit hash + subject
- Confirmation the logger pattern is now at line 9
- Smoke-import passed
- Ready for commit 3 (moments.py) to mirror the same pattern
</output>
